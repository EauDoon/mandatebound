# README Quick Start Audit (phase 15, 2026-09-23)

Verdict: added.

`README.md` opened with a long `## Flagship story` narrative that buried the
only runnable commands (`npm run build` then `node examples/case-study.mjs`)
under a 30+ line case-study walkthrough. Inserted a `## Quick start` section
before the flagship story using the public, non-invented commands declared in
`package.json`: `npm install`, `npm run build`, `npm run demo`. Engine
constraint (`engines.node >= 22.12.0`) is sourced from `package.json` and
matches the existing `## What's new` prose.
